import { shallowMount } from "@vue/test-utils";
import swiperView from '@/views/userLayout/swiperView.vue'
describe('SwiperView Testing', () => {
  let wrapper = null
  test('should ', () => {
    wrapper = shallowMount(swiperView)
  });
});
